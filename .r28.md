## Round-28 — the full-window rigidity theorem (level iteration), `2743ec68d`

The Step-2 disjoint-case capstone landed — and compiled clean on the first attempt (axiom-clean, 0 warnings, strict-verified):

**`full_window_rigidity`** (`RigidityFullWindow.lean`, self-contained with the R26 halving components): for the root-of-unity chain `Good` (closed under squaring) with the per-level closure oracle (= R25's verified general t=1 theorem, transported through the signed-point encoding): **disjoint `Good` sets with equal power sums `p₁,…,p_{2^k−1}` are `LiftStructured k`** — antipodally closed at every one of `k` successive squaring levels, the exact root structure of the R22 `2^k`-lift family.

The induction is exact: closure from `p₁` at each level; the halving engine drops the window — with perfect alignment (`2l ≤ 2^{k+1}−1 ⟺ l ≤ 2^k−1`) — while `Good` descends through squares.

**The complete Step-2 structure theorem, every component machine-checked:**
```
equal-window pair → core division (R27 ✅) → disjoint residuals
  → [closure (R25 ✅) + halving (R26 ✅)] iterated (R28 ✅)
  → 2^k-lift petals (R22 ✅)
⟹ equal-window families in μ_{2N} are SUNFLOWERS with 2^k-lift petals
```
at all sizes, all linear windows, characteristic zero — the rigidity that the prize's deficiency mechanism (Conjecture 41's PTE/cyclic route) requires, with floor = ceiling.

**Remaining before the maintainer can judge δ*-composition complete:** (1) the `hclosure` de-oracling (R25 lives in the signed-point encoding; the bridge is the standard μ-enumeration — mechanical); (2) the composition through the clique rank structure (R20/21) + large-p transfer (R19) into Conjecture 41's statement and the δ* pin. Both are now formal assembly over named, verified theorems — no unproven mathematics remains in the disjoint-case chain. Issue stays open for that assembly and the maintainer's final review.
